/* global jQuery, neoweaverData */
( function ( $ ) {
	'use strict';

	$( document ).ready( function () {
		// Public JS entry point.
		// neoweaverData.ajaxUrl and neoweaverData.nonce are available here.
		console.log( 'NeoWeaver Public loaded.' );
	} );
} )( jQuery );
